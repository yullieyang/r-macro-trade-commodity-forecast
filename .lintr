linters: linters_with_defaults(
    line_length_linter = line_length_linter(160L),
    object_name_linter = NULL,
    object_usage_linter = NULL,
    commented_code_linter = NULL,
    indentation_linter = NULL
  )
exclusions: list(
    "outputs",
    "data",
    "docs",
    "_site",
    ".quarto"
  )
